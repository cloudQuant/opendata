"""Unified data contracts (design §4).

This package is the self-developed core of the data platform: standardized
models that are independent of any data source, the domain registry, and
the shared HTTP client. It is the schema upstream for both dwd tables and
the REST/WS API (FR-2).
"""
